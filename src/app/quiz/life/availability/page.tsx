import { LifeQuizStep } from "@/components/life-quiz-wizard";

export const metadata = {
  title: "Availability · Life Quiz | Click",
};

// Step 2/4 · Availability.
export default function AvailabilityPage() {
  return <LifeQuizStep step={1} />;
}
