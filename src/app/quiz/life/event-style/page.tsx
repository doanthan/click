import { LifeQuizStep } from "@/components/life-quiz-wizard";

export const metadata = {
  title: "Event style · Life Quiz",
};

// Step 3/4 · Event style.
export default function EventStylePage() {
  return <LifeQuizStep step={2} />;
}
