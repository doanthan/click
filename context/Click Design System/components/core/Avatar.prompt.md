A round avatar for a person at an event - image via `src`, or a warm gradient initials chip. Pass first name only (the product never shows surnames).

```jsx
<Avatar name="Mia" size={44} />
<Avatar name="Jules" src="/uploads/jules.jpg" ring />
```

Props: `name`, `src`, `size` (px), `ring` (lavender mutual-click ring).
