A labelled text field with optional helper text and leading icon; lavender focus ring on a cream-white field.

```jsx
<Input label="Email" placeholder="you@email.com" type="email" />
<Input label="Email" error helper="That doesn't look right" />
```

Props: `label`, `helper`, `iconLeft`, `error`, plus all native input attributes.
