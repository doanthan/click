<!-- People-card component spec — the canonical single-source-of-truth for the "person you can click with" card. Updated 29 Jun 2026 (FOLDED IN the refined visual system so this component is self-contained — one 3-level type scale [name 17–18 / context 14 / intent 13, context a step above intent], one avatar size [52] across all sections, ONE earned accent [soft --lavender-wash #F0ECF4 background fill on your-move cards only; everything else neutral white, section header carries state; never a full-strength fill or 3–4 accent colours; supersedes the 3px left-rule, Cindy 29 Jun]; Past-clicks NAMES THE PLAN "You went to [Plan] together"; RELEASED past-clicks COLLAPSE into a "+ N past clicks" line, not standing faded cards [released ≠ dormant; dormant removed]. Also: SANCTIONED GRID VARIANT [default one-per-line rows; Who-was-there = the one 2-up exception, never a centred tile]; fixed the stale "3-up grid" reference to one-per-line). Originally 27 Jun 2026. Created to fix the audit gap (the people card was described twice — in ClickMechanic §E and Dashboard — and drifted into the broken squished-column screen). ONE component, reused identically on the Click-with-someone page, the dashboard, and inside the profile modal header. Responsive WEBSITE. Distinct from the EVENT card. Refs: CLICK_PALETTE / CLICK_TYPE / CLICK_LANGUAGE / Buttons_Tags / 09_CLICK_WITH_ME_AND_RADAR / 21_CLICK_MECHANIC. -->
# Click — People Card (the "person you can click with" card, all states)

The people card is the second-most-repeated component in the product (after the event card) and the one that was rendering **broken** — squished narrow columns, titles wrapping vertically, bios dumped on cards. This spec makes it a **single canonical component, reused identically** on the **Click-with-someone page** (3 cards), the **dashboard** ("click with someone" section — 1 rotated card), and as the **header of the profile modal**. Like the event card, every instance is pixel-consistent — that consistency is the fix.

**It is NOT the event card.** Do not reuse event-card chrome (no dominant 16:9 banner, no price footer, no RSVP). A person is a face + the real overlap + one intention — a calmer, more human card.

**MDs this references:** `CLICK_PALETTE.md` (colour), `CLICK_TYPE.md` (type scale), `CLICK_LANGUAGE.md` ("click with" grammar, banned words), `Click_Design_Prompt_Buttons_Tags.md` (neutral interest chips + buttons), `TECH/09_CLICK_WITH_ME_AND_RADAR.md` §2 (card content rules), `TECH/21_CLICK_MECHANIC.md` §7B (already-clicked card states).

**Five locked rules before the prompt:**
- 🔴 **No bio / prompt text on the card.** A person's bio, prompts, and full interests live ONLY in the **profile modal** (opened via "View profile"). The card carries the *overlap*, never the essay.
- 🔴 **Commonality line carries a NON-INTEREST commonality — it must NOT duplicate the interest tags (Cindy 29 Jun).** Pick the FIRST available, in order: **(1) a real shared EVENT** ("You were both at [event]"); **(2) shared MUSIC** ("Both into jazz & house" — a different axis from the interest tags, the everyday workhorse, using `profiles.music_tags`); **(3) POST-MUTUAL ONLY, a shared light NON-SENSITIVE life tag** ("Both new in town", "Both new parents", "Both pet owners"); **(4) PROXIMITY** ("you're both nearby" / "people near you" — from a postcode/distance RANGE, NEVER a named suburb; city-agnostic so it scales). 🔴 **NEVER "Both into [the same interests]" as a line above the same interest tags** — that's duplication; shared INTERESTS live in the tags ONLY (a small overlap glyph on the tag row signals "in common"). If none of (1)–(4) exist, OMIT the line — never a bare line, never fabricated. 🔴 **NEVER surface the private quiz persona / social-style answers** (recharge style, room behaviour) — promised private + too personal; they tune matching invisibly only. Life tags are PRIVATE until mutual: pre-mutual (discovery) the line is event or music only; the post-mutual card may use the light life tag.
- 🔴 **The click button is ONE control across states — same size + shape, only fill colour + label change.** Default → pending → mutual must occupy the **identical button footprint** (do NOT swap the button for a smaller pill — that was the live-render bug where pending rendered as a different shape). State is signalled by **fill colour + label (+ at most one ✨)**, never by resizing/reshaping, and never by colour alone. Per `Click_Design_Prompt_Buttons_Tags.md` (stateful action button).
- 🔴 **Pending must read as PENDING, not confirmed:** on click the button switches to its **pending treatment** — same footprint, a **quiet/muted fill** (NOT the celebratory filled-purple or a bright lavender pill), label **"clicked"** (NO sparkle — pending is not a peak). Derived from the viewer's OWN click only. **NEVER "You clicked with [Name]"** or any phrasing/treatment that reads as confirmed/mutual — it never names the person and never implies they clicked back. (The celebratory treatment belongs ONLY to the *mutual* state "clicked ✨".) A click is a quiet, anonymous, one-way pending action — low-stakes and unresolved, not a match made. **NO anonymous helper line on the card** — see next rule.
- 🔴 **The anonymous reassurance shows ONCE at the top of the section/page, NEVER on a card.** "🔒 Clicking is anonymous — we'll only show you if it's mutual." sits once above the set; it must NOT be repeated under any card (including the pending card) — repeating it breaks the card layout. (Fixes the live render that put it under the pending card.)
- 🔴 **Max ONE ✨ per element, ✨ marks the mutual peak only.** Pending carries NO ✨ ("clicked"); the **mutual** state carries one ("clicked ✨"). The shared-context "Both into…" line uses a plain overlap glyph, **not** a ✨. No double sparkles, no decorative sparkles — ✨ is reserved for the peak, never decoration.
- 🔴 **No age on the card.** The card shows the NAME only (no "· 29"). Age is sensitive and front-loading it reads dating-app-y and invites snap judgments — it lives on the **profile modal** only. The card leads with the overlap, not demographics. (Researched 27 Jun: collect-minimal / reveal-on-deeper-view; fits Click's intent-neutral, activity-first brand.)
- 🔴 **Intent labels are sentence case:** "Here for friends", "Open to dating", "Here for the activities" — NEVER title case ("Here For Friends", "Open To Dating"). (The live render title-cased them — fix it.)
- 🔴 **One person per line on the page.** The 3 people render as **3 full-width horizontal ROW-cards stacked vertically (one per line)** on desktop — NOT a cramped 3-up column grid (which clipped the buttons and staggered the footers). See LAYOUT.

```
=== PROMPT (paste under the GLOBAL block from Click_Design_Prompt_FullBuildOut.md) ===
ROLE: Principal product designer (reference-class consumer — Hinge / Coffee Meets Bagel curated-discovery craft, Luma calm). Design Click's PEOPLE CARD — the "person you can click with" card — as ONE canonical component for a responsive WEBSITE (375 → 1440), reused identically on the Click-with-someone page, the dashboard, and as the profile-modal header. NOT a native app (no native chrome). Calm, human, warm; the overlap is the hook. It is DISTINCT from the event card — do not reuse event-card chrome.

=== ANATOMY & VISUAL HIERARCHY (the elements; arrangement is responsive — see LAYOUT) ===
The same elements, laid out as a horizontal ROW on desktop (avatar left · info center · actions right) and a VERTICAL card on mobile (avatar top → info → actions). Reading order is always: who they are → the real overlap → the action.
1. AVATAR — rounded, ~56–64px (compact, per GLOBAL density), warm-graded real photo (never stock/3D blob). Left on the desktop row, top on the mobile card — consistent across all instances. A subtle Mist ring; NO status dot, NO online indicator.
2. NAME — ONE line: "Mia" (first name only; **NO age on the card** — age lives on the profile modal). Use the **`card-title` type token (Poppins SemiBold 18 / 24)** per CLICK_TYPE — Ink (same token as the event-card title, for cross-card consistency). Must NEVER wrap vertically — truncate gracefully if absurdly long.
3. INTENT LABEL — directly under the name: "Here for friends" / "Here for the activities" / "Growing my circle" / "Networking" / "Open to dating" (system 13, Slate). **Sentence case ALWAYS** — never title-case. Desire-framed, never a status, never a badge colour. 🔴 **"Open to dating" shows ONLY when BOTH the viewer and this person have dating mode on** (per CLICK_LANGUAGE §6.3 — site-wide; a friends-only viewer NEVER sees "Open to dating" on anyone; show that person's other intent or just their interests instead). Foreground friends/activities/networking — dating is one of several, never the majority of a set.
4. COMMONALITY LINE — the hook. CONDITIONAL, one line, system 13–14, Ink, with a plain overlap/link glyph (NEVER a ✨). 🔴 It carries a **NON-interest commonality so it never duplicates the interest tags** — pick the FIRST available: (1) shared EVENT "You were both at Wheel throwing"; (2) shared MUSIC "Both into jazz & house"; (3) post-mutual only, a light life tag "Both new in town" / "Both pet owners"; (4) cluster proximity "you're both nearby". If none, OMIT the line. NEVER "Both into [the same interests as the tags]". NEVER the private persona/social-style answers. NEVER fabricate.
5. SHARED INTEREST TAGS — up to 3 neutral chips (white fill, Mist hairline, Ink text — per Buttons_Tags), "+N" if more. These are the SHARED ones (intersection with the viewer) — a small overlap glyph on the row signals "in common"; they are the ONLY place shared interests appear (the line above never re-states them). NO life tags on the card pre-mutual (sensitive/private — profile/mutual only).
6. ACTIONS — right side of the desktop row (vertically centered) / full-width at the bottom of the mobile card: PRIMARY "click with [Name]" (Deep-Purple filled, Poppins label, single line — never wraps/clips) + SECONDARY "View profile" (quiet ghost/text → opens the profile modal; beside the primary on desktop, below it on mobile). One primary action only. In the PENDING state the PRIMARY button KEEPS its exact size + shape and switches to its muted pending treatment labelled "clicked" (colour + label change only — NOT a different-shaped pill, NO sparkle, NO helper line under the card); "View profile" stays as-is.
   NO bio, NO prompts, NO free text anywhere on the card.

=== LAYOUT & ALIGNMENT (ONE PERSON PER LINE — this is the fix for the broken 3-up) ===
The card is RESPONSIVE with two arrangements. The 3-up column grid is DEAD (it clipped the buttons, wrapped "click with [Name]" onto two lines, and staggered the footers).

- **Desktop / tablet ≥768 — HORIZONTAL ROW (one person per line):** the 3 people stack VERTICALLY as 3 full-width row-cards inside a capped container (~760–860px, left-aligned on the page, not full-bleed). Each row: **avatar left** (~64–72px) · **info column center, flex-grow** (name·age → intent → conditional shared-context → ≤3 tag chips) · **actions right** (the "click with [Name]" + "View profile" pair, vertically centered). Because each row owns the full content width, the two actions ALWAYS fit on one line and never clip or wrap. Rows are independent, so height differences between people never cause misalignment. Generous row padding (~20–24px), rows separated by whitespace or a single Mist hairline (not nested boxes).
- **Mobile <768 — VERTICAL card:** the row collapses to the vertical arrangement (avatar top, then name [no age] → intent → shared-context → tags → actions full-width, primary above secondary). Single column, full-width, stacked — exactly the mobile render that already works. ≥44px targets.
- **Actions never wrap or clip, ever:** "click with [Name]" is a single-line label (give it room; if a name is very long, truncate the name, never wrap the button). "View profile" sits beside it (desktop) or below it (mobile) as a quiet ghost — never pushed past the card edge.
- Spacing: 8pt grid; internal padding ~20–24px (desktop row) / ~16–20px (mobile card). One radius (16px); one soft low shadow OR a 1px Mist hairline, never both. Ink never pure black; titles/labels clamp, never stack letter-by-letter.
- The single dashboard instance uses this SAME row-card (one row, one person) — see Dashboard prompt.

=== STATES (mock every one) ===
1. DEFAULT — as above; the commonality line shown in its variants (one card with a real shared event, one with shared music, one with the line omitted so the interest tags stand alone) to prove equal height.
2. PENDING (you've clicked — the usual outcome) — the primary button stays the SAME size + shape and switches INLINE to its muted pending treatment: a quiet fill (e.g. Mist/Lavender-tint with Deep-Purple or Slate label — clearly NOT the filled-purple "go" state, clearly NOT a celebratory state) labelled **"clicked"** (NO sparkle). **NO helper line under the card** — the anonymous reassurance is shown once at the top of the section only. Derived from your own click only; never reveals theirs. **UNNAMED and reads as pending — NEVER "You clicked with [Name]"** and NEVER a different-shaped pill. "View profile" stays available. The card does NOT reshuffle mid-session. This is the calm, low-stakes "sent, now wait" state — not a celebration, not a confirmation.
3. ACTIVE MUTUAL — if you already have a live mutual with this person: the SAME button switches to its mutual treatment (Sage fill) labelled **"clicked ✨"** (one ✨ — the peak) and taps through to the mutual. (Rare on a discovery surface; include for completeness — mirrors 21 §7B. The big "You clicked with [Name]." headline belongs to the separate mutual-reveal moment, not this chip.)
4. LOADING — a skeleton matching THIS card's shape (avatar circle + 2 text bars + chip row + button bar), not a spinner.
5. NOT SHOWN — suppressed / expired / blocked people never render as a card (handled upstream; no dead/disabled card).
(Anonymous reassurance "🔒 Clicking is anonymous — we'll only show you if it's mutual" appears ONCE at the top of the section/page — NEVER on a card, not even the pending card. Repeating it per-card breaks the layout. See the page-level prompts.)

🔴 **NO name-adjacent state PILL on this card (Cindy 28 Jun).** When this card is reused as the Your-clicks "outcome card" (Live mutuals / Plans / Past clicks), the SECTION HEADER + the quiet state accent + the action carry the state — do NOT add a pill beside the name ("You two clicked" / "You both clicked" / "You're both going" / "We clicked 🔥" / "Released gently") and do NOT put a ✨ on a list card (the list is not a peak). The card leads with the NAME as its title. "We clicked 👍" is a closure ACTION (button) on a Past-clicks card, never a passive status pill.

🔴 **REFINED VISUAL SYSTEM for the Your-clicks outcome card (Cindy 29 Jun, research-backed — this is what makes the cards look professionally designed, not stale):**
- **One fixed 3-level type scale, no two rows competing:** name = `card-title` (Poppins SemiBold ~17–18, Ink) → the context/plan line ~14 Ink → the intent line ~13 Slate. 🔴 The context line must be a STEP larger/darker than the intent line so they don't read as one flat block (prior renders had three text rows at near-identical size+weight = no hierarchy).
- **One avatar size (~52) across ALL three sections** — never shrink per group (Live mutuals = Plans = Past clicks, same avatar).
- 🔴 **ONE earned accent — a soft `--lavender-wash` (#F0ECF4) BACKGROUND FILL on YOUR-MOVE cards ONLY** (open "Suggest a plan" / their-proposal "See their plan") — the whole card is gently "lit up" so the cards that need the user read at a glance (Cindy 29 Jun — supersedes the 3px left-rule, which was under-powered; the wash gives area-level emphasis without a loud saturated fill). A-plan, waiting ("Waiting on [Name]"), and Past-clicks cards are the **clean NEUTRAL white card** — no wash, no rule; the section header carries their state. 🔴 NEVER a full-strength `#C8B8F8`/`#3B2F81` card fill (too loud, fails the calm brand + forces white text), NEVER 3–4 different accent colours. Slate text on `--lavender-wash` = 4.74:1 (AA pass — verified), so the card content is unchanged. The three-level hierarchy: resting card = white · your-move card = `--lavender-wash` fill · the action = the filled-purple button (the button stays the real "do this" affordance; the wash is the at-a-glance scan cue). Drop the left-rule — one emphasis mechanism, not two.
- 🔴 **Intent line — IDENTICAL treatment to the discovery card (Cindy 29 Jun): grouped tight with the NAME, Slate (~13), sentence case — NEVER green, never a separate bulleted line lower down.** The mutual/outcome card uses the shared wording ("You're both here for friends") vs the discovery card's solo "Here for friends" — only the WORDS differ (solo vs shared), never the colour or the grouping. 🔴 Sage/green is reserved for SUCCESS status ONLY (the mutual "clicked ✨" marker, "you're going") — green on the intent line is fragmented consistency + dilutes the semantic colour. The warmth on a mutual card comes from the commonality line ("Both into …") + the wash + the action, not a coloured intent line. Drop the leading dots on the outcome-card lines — whitespace grouping + the overlap glyph carry the structure.
- **Proximity grouping:** name + intent grouped tight (~2–3px), then a larger gap (~7px) before the context/plan line.
- **Per-state context line:** Live mutuals (open) → NO event line (no plan yet); their-proposal → "[Name] suggested [Event] · [time]"; you-proposed → "You suggested [Event]" + muted "Waiting on [Name]"; Plans → "Going to [Event] · [when]"; Past clicks (connected) → 🔴 **NAME THE PLAN: "You went to [Plan] together · [when]"** (fall back to "you clicked at [event]" only if no shared plan).
- Footers/actions right-aligned and aligned across every row; 16px inter-card gap set ONCE on the container (identical in every section).

🔴 **RELEASED ("Still out there") past-clicks COLLAPSE (Cindy 29 Jun) — do NOT render as standing faded cards.** Connected wins ("We clicked 👍") stay visible as Past-clicks cards; released (7-day soft-expire) entries tuck into a single quiet **"+ N past clicks"** line that expands only on tap (a no-action melancholy card clutters the momentum surface). Re-clickability preserved (the line expands; when expanded the released entry keeps the neutral "Still out there - if you cross paths again, you can pick it back up", never "expired"/loss-framed). 🔴 Released ≠ dormant — dormant was removed entirely; a live mutual is always the actionable "Suggest a plan" card.

=== INTERACTION & MICRO-ANIMATION ===
- Tap "click with [Name]" → a gentle pulse (~250ms) → the button switches INLINE to its pending treatment (same size + shape; muted fill + "clicked" label, no sparkle), no page load, no spinner. Optimistic — never wait on the server. This is an everyday-feedback moment (100–300ms), NOT one of the three ✨ peak moments — keep it soft, not a celebration.
- "View profile" → opens the profile **centered modal** (centered overlay ~600–640px on desktop / full-width sheet on mobile, per `Click_Design_Prompt_Profile.md` Mode B), no navigation. The modal uses a Close (✕) — Esc / scrim dismiss it, never the page (per GLOBAL nav rule).
- Hover (web): card lifts ≤2px / avatar ring warms, 150–300ms. Visible keyboard focus ring (Deep Purple #3B2F81 with a cream offset — lavender fails 3:1 on cream). Respect prefers-reduced-motion (swap motion for a static pending state).

=== RULES ===
- "click with [Name]" — never bare "click", never "match", never "click on". Verb is always "click with" (lowercase). **Single space only — "click with Mia", NEVER "click with  Mia" (no double space before the name).**
- 🔴 BUTTON CONSISTENCY (the live render had it ghost on the dashboard, filled on the people page — fix): the primary "click with [name]" is **ALWAYS the same style on every surface — filled Deep Purple**. Never ghost/outline on one screen and filled on another. "View profile" is always the quiet ghost secondary.
- 🔴 STATEFUL, ONE FOOTPRINT (the live render shrank pending into a different-shaped pill — fix): the click button keeps the **same width-band/height/radius** across default → pending → mutual; ONLY the fill colour and label change (default = filled Deep Purple "click with [name]"; pending = muted "clicked", NO sparkle; mutual = Sage "clicked ✨"). Never resize or reshape between states. ✨ on the mutual state only. Per `Click_Design_Prompt_Buttons_Tags.md`.
- 🔴 COMPACT (per GLOBAL density — the cards were running too large): row padding ~16px; avatar 56–64; **button height ~40px (≥44 tap area via padding), label 14–15, AUTO-WIDTH inside the desktop row (not full-width), full-width only on mobile**; name = `card-title` 18/24, intent/context 13–14, chips compact. Tighten so 3 rows read calm and dense, not bulky.
- NO bio/prompt text on the card (that's the profile modal's job). NO life tags on the card. Shared-context line never fabricated.
- One primary action (Deep-Purple "click with"); "View profile" is quiet secondary. Selected/primary is always Deep Purple, never a status colour. No gradients.
- Cream or white card on cream canvas; Poppins for name; system font for intent/context/chips; Ink never pure black; refined line icons (Lucide/Phosphor), no emoji-as-icon (keep locked ✨).
- Real Sydney people, FIRST NAME ONLY on the card (no age, no surname): Mia, Tom, Priya, Jules, Hassan, Bec. (Age shows on the profile modal, not the card.) Real shared interests (pottery, live music, run clubs, wine, cooking). No placeholders, no fake bios.

=== DELIVERABLE (mock every state; PROVE equal height + alignment) ===
Desktop = 3 full-width ROW-cards stacked one-per-line in a capped container; mobile = vertical cards stacked. Mock: DEFAULT row with a REAL shared-event line (Mia — "You were both at Wheel throwing"), DEFAULT rows with a NON-interest commonality line that does NOT duplicate the interest tags (Jules — shared MUSIC "Both into jazz & house"; Tom — cluster PROXIMITY "you're both nearby"), and a row with NO non-interest commonality (the line simply omitted, the interest tags stand alone — prove there's NEVER a bare "You were both at" and NEVER "Both into [the same interests as the tags]") · PENDING (the SAME button footprint in its muted treatment, label "clicked" — NO sparkle, NO helper line under the card, unnamed — prove it's the same size/shape as default, only colour+label changed, and NOT "You clicked with [Name]") · ACTIVE MUTUAL (same footprint, Sage "clicked ✨") · loading skeleton. **Place the three states side by side so the identical footprint is obvious.** The anonymous reassurance appears ONCE at the top of the page, never on a card. NAME only on cards (no age). Confirm: ✨ on the mutual state only (none on pending, none on the "Both into…" line). Use short AND long names/overlaps to prove the actions never wrap or clip. Intent labels sentence case. Show at 375 + 1024, micro-animation noted.
=== END PROMPT ===
```

---

## ONE CARD, FOUR SURFACES — the consistency matrix (single source of truth)

🔴 **The People Card is ONE component. The table below lists the ONLY things allowed to differ per surface; EVERYTHING else is identical.** (Cindy 29 Jun — the dashboard card had drifted: name/intent stacked, no commonality line, no "View profile", a smaller avatar. Pull it back to the invariants.)

**INVARIANTS — identical on every surface (a card must look the same wherever it appears):**
- Avatar LEFT, ONE size (~52) — never shrink it per surface.
- **Name + intent grouped TIGHT and INLINE** — name `card-title` ~17 Ink · intent ~13 Slate, sentence case. 🔴 NEVER stacked as two separate lines (the dashboard render stacked them — fix), never green.
- A **CONDITIONAL commonality hook line** that carries a NON-interest commonality (so it never duplicates the interest tags): first available of (1) shared EVENT, (2) shared MUSIC, (3) post-mutual light life tag, (4) cluster proximity — with the overlap glyph; omit cleanly if none. NEVER "Both into [the same interests]"; NEVER the private persona answers. (Full rule in locked-rule #1 above.)
- ≤3 shared-interest tags (neutral 22/12, one line + "+N").
- Same type scale, padding (~16–20), equal-height, whitespace/hairline grouping, and the stateful click-button footprint (default → muted "clicked" → Sage "clicked ✨"). First name only, no age.

**SANCTIONED VARIATIONS — only these may change per surface:**

| Surface | Count / layout | Actions |
|---|---|---|
| **Click page** (discovery, `ClickMechanic §E`) | 3, one-per-line full-width rows | "click with [Name]" + "View profile" — RIGHT side (room on the wide row) |
| **Dashboard** ("click with someone") | 1 rotated card + "See everyone →" | "click with [Name]" + "View profile" |
| **Who-was-there** (post-event) | 2-up grid (1-up mobile), "Show more" past ~12 | "click with [Name]" + "View profile" — paired BOTTOM row (narrower card) |
| **Event unlocked — attendee list** (`EventDetail`) | grid of attendees | 🔴 NO "click with" (clicking is post-event only); the WHOLE CARD → profile modal; shared INTERESTS only, no life tags |

🔴 **Only the action LAYOUT adapts to card width** — right-side on the wide one-per-line rows, paired bottom-row on the narrower 2-up/dashboard — but the action PAIR (click-with primary + View-profile GHOST) and the anatomy never change. The attendee-list card is the ONLY one with no click action (context-only; whole card opens the profile modal).

- **Profile modal header** (`ClickMechanic §F`) reuses the card's avatar / name / intent / commonality, then adds age + bio + full interests.

If the card changes, it changes **HERE**, and every surface inherits it. Do NOT re-describe the card in those files — reference this one.

## Notes for Cindy
- **This is the fix for the broken screen.** The event card never broke because it had one canonical spec; the people card broke because it was described in two places and drifted. Now there's one source of truth.
- **The hook is the overlap, not a bio.** Research (Hinge/Coffee Meets Bagel) shows a small, curated, *reasoned* card ("here's why you two") drives more intentional action than a profile dump — and it keeps the card scannable and equal-height. The bio waits in the modal for anyone who wants more.
- **Equal height is engineered, not hoped for:** flex-grow content + min-width columns + 2-line clamps mean the cards can't squish or stagger again.
- **Want me to fold the EventCard + PeopleCard into a tiny "components" index** so Discovery/Dashboard/Click all point to the two canonical card specs? That'd close the consistency loop the audit flagged.
