<!-- CORRECTED + CONSOLIDATED design-system README for the claude-design project. 27 Jun 2026. Paste this in to REPLACE the project's current README.md. It mirrors the canonical Click specs (CLICK_PALETTE / CLICK_TYPE / CLICK_LANGUAGE + UIUX/Cowork/*). On any conflict, those canonical specs win and THIS file is corrected to match - never the other way around. -->
# Click - Design System (canonical mirror)

> **click** *(verb)* - that effortless feeling when you just click with someone. The activity is the icebreaker; connection is the outcome.

This is the design system for **Click**, an invite-only events platform launching in inner Sydney. It mirrors Click's canonical specs and is what this project reads first - so it must stay in sync with them.

---

## 0. HOW THIS SYSTEM IS GOVERNED (read first - this stops stale, contradictory pages)

1. **This README is a MIRROR of canon, not an original interpretation.** On ANY conflict, the canonical specs win - `CLICK_PALETTE.md` (colour), `CLICK_TYPE.md` (type), `CLICK_LANGUAGE.md` (words), and the per-screen prompts in `UIUX/Cowork/`. When they disagree with this file, **this file is wrong and gets corrected** - never the reverse.
2. **Update this README whenever a rule, token, or component changes - in the same pass.** A stale README is the #1 cause of drifted renders. If a rule changes, change it here too, immediately.
3. **Every change applies to ALL pages, in the same pass.** Never fix one screen and leave the rest stale. Change the tag → change it everywhere tags appear. Change the card → re-render every surface that uses the card.
4. **One component, used IDENTICALLY site-wide.** Button, Tag, Badge, Avatar, **Event Card**, People Card, the top nav, and the footer are SINGLE components. Never restyle one per page. 🔴 **The EVENT CARD is the SAME CARD THROUGHOUT THE SITE** - discovery, dashboard "suggested"/"saved" strips, my-events, landing - identical layout, states, sizing, one-line tags, pinned footer. (Click Radar is the exception: it is NOT event cards but compact social-proof BARS - see §5.)

---

## 1. PLATFORM - responsive WEBSITE, not an app (yet)

Click is a **responsive website, mobile-optimised** (375 → 768 → 1024 → 1440). The native app is a LATER phase. **Strip all native-app chrome** - no device status bars, safe-area insets, native tab bars, splash screens, or push-permission dialogs. The phone frame in mockups is presentation only. Use web patterns: a responsive sticky header; an optional sticky bottom action bar on mobile is a web pattern, not a native tab bar.

*(This corrects the old framing of "a consumer app, mobile-first, ~420px frame." It's a website.)*

---

## 2. CONTENT FUNDAMENTALS (binding - `CLICK_LANGUAGE.md` overrides on conflict)

**Voice:** a friend who knows the city well, has good taste, doesn't oversell. Warm, specific, a little dry, confident without being loud. Speaks to "you"; "we" only lightly. Never therapeutic, never lectures.

**Desire, not deficit.** Name the appetite, never the lack. ✅ "Some of the best people you'll ever meet - you just haven't crossed paths yet." ❌ "It can be hard to meet people."

**The capitalisation split (functional, not cosmetic):** **Click** (capital C) = the platform, in prose. **click** (lowercase) = the feeling/mechanic (= connect/hit-it-off) - this is the ENCOURAGED core verb. Only the literal button-tap sense is banned (use **tap**/**select**). The wordmark is lowercase `click`.

**Lowercase UI chrome (the mechanic chrome follows the feeling):** nav **"✨ click"**, page header **"click with someone"**, action button **"click with [name]"**, pending pill **"clicked"** (see below). Capital "Click" only as the platform name in prose/wordmark. Otherwise prose is sentence case.

**The one grammar rule:** always **"click *with* someone"**, never "click on someone."

**Punctuation - hyphens, never em-dashes (binding, site-wide):** never use an em-dash (`—`); use a spaced hyphen ` - ` for the same break, everywhere - copy, prose, comments, data, docs. No en-dashes for ranges either; write "25 to 38". This applies to every page, component, and file in the system.

**Banned - fail on sight:** `match`/"it's a match" (→ mutual click), `swipe`, `connect`/`connection` for the mechanic, `lonely`/`isolated`/`struggling`, "find your tribe"/"meet your people"/"meaningful connections", "buy a ticket"/"RSVP to unlock"/price-in-button (→ a single **"RSVP"** for ALL events, price on the card; "Join waitlist" when full → "Joined waitlist" once joined; "View details"/"You're going" when booked), "refer a friend" (→ "bring your people into Click"), **founding-merchant** anything (hosting is free during the Sydney pilot - no founding programme/badge/commission), and **"click" as a UI verb**.

**No "just."** Audit every "just" before an intent/persona - it demotes.

**Opportunity, never loss.** Lifecycle copy never says "expire/last chance/winding down/you missed your chance." Soft-release (locked): "Still out there - if you cross paths again, you can pick it back up."

**"In real life / IRL" - encouraged brand phrase.** The thesis: real connection is built in person, over shared activities, more than once. Spell out **"in real life"** in primary/explanatory copy; **"IRL"** for social/short/playful. The handle is **@click.irl**. Don't overuse (one strong "in real life" per page).

**Acquisition CTA = "Request an invite"** (Landing + How It Works; invite-only brand - active/exclusive framing). Distinct from the per-event "Join waitlist" action. **Never block an out-of-area signup** - anyone in Sydney can request an invite; an out-of-area suburb/postcode still completes and confirms "we'll tell you the moment Click reaches you" (Landing + onboarding, same promise).

**Rejection-proof (describing the click - binding, site-wide).** **Lead with desire; let safety be felt, not named.** On **acquisition / marketing surfaces (Landing, How It Works)** keep the click *curious* - tease it, don't explain the machine - and put **no rejection on the page in either direction**: ❌ don't plant the could-be-no ("tells you if it was mutual", "if they feel the same", "find out if they liked you back", "they'll see you clicked"), AND ❌ don't over-correct by naming the safety ("no one gets turned down", "nothing is rated/ranked", "you're never on display") - both put rejection on the page. On **mechanic surfaces** (the actual click UI), if the rule must be stated, state it once, neutrally and upside-first: ✅ "you only ever hear about the people who clicked with you too" · "a one-way click is never told to anyone." And **welcome quiet readers by showing the calm, never by labelling them** - never "for the shy/lonely/introverts/anxious", "beat your social anxiety", "find your tribe." Desire-led identity only ("you'd rather meet people in real life than on the apps"); show it (activity-first, private, you control your visibility), don't name the wound.

**✨ usage (locked):** max ONE ✨ per element, never decoration; **always the Deep-Purple brand glyph (the `Spark` component, purple/lavender), NEVER the raw orange/multicolour emoji** (the emoji collides with the Amber status colour). The **pending** click state carries NO ✨; the **mutual** state carries one. Concentrate ✨ at the three peaks (mutual / both-going / connected) and the nav "✨ click". 🔴 **Never on a list/utility surface** - NOT on a Your-clicks row, NOT on the finish-setting-up card, NOT on a coordination context line ("Matched to you both" uses a plain glyph), and the **Click Radar row's leading icon is a plain radar/signal line-glyph, never a ✨**.

**Locked mechanic strings (don't improvise - placeholders ship):**
- Post-event headline: **"Did you click with anyone?"** *(corrects the old "Who'd you click with?")*
- Pending one-way click state: **"clicked"** - the SAME click button in a quiet/muted treatment (no ✨, same footprint as "click with [name]", NOT a smaller pill). Reads pending/unresolved.
- Mutual persistent card-state: **"clicked ✨"** (Sage). The big reveal headline is **"You clicked with [Name]."** (a separate moment).
- Mutual push: **"It's mutual - you clicked with [Name]. ✨"**
- Intent line on a mutual: equal → "You're both here for [intent]."; mixed → show both, never collapse.
- Anonymous reassurance: **"🔒 Clicking is anonymous - we'll only show you if it's mutual."** - shown ONCE at the top of a people section, NEVER under each card.
- Closure button: **"We clicked 👍"** → "Love that. That's what Click's for. ✨".

---

## 3. VISUAL FOUNDATIONS

**Palette (`tokens/colors.css` must match):** Deep Purple `#3B2F81` = brand spine + the ONLY primary-action / selected colour (flat, never a gradient or glow). Cream `#F9F6F0` = page ground (never stark white). Lavender `#C8B8F8` = soft lift (washes, accent circles). Ink `#1C1830` = text (never pure black). Slate `#6B6580` = meta. Mist `#E8E4F0` = hairlines/dividers/disabled. **Status colours live on BADGES ONLY** - Coral `#E8674C` (almost full), Amber `#E0A33A` (trending/waitlist), Sage `#5B8C6E` (free/going/mutual), Teal `#2E7D8A` (new) - never a CTA, hero accent, or category. No apricot. No rainbow category palette. Button hover `#332873`, pressed `#2A2160`.

**Lavender - accent, never a canvas (locked):** brand lavender `#C8B8F8` (`--lavender-300`) is an **accent / small-surface** colour only - icon circles, chips/tags, hover washes, the swatch ramp, small highlights. It is **NEVER a section-sized background** (a big cool field on warm cream reads as a clash, and Slate `#6B6580` on it is only 3.07:1 - fails AA). Large section bands and big tinted panels use **`--surface-section`** (= `--lavender-wash` `#F0ECF4`, a derived low-chroma warmed tint that bridges cream→lavender→purple; Slate on it = 4.7:1, passes AA). `--lavender-wash` is a derived surface tint, NOT a brand colour. (Re-pointed: the How It Works section bands + the dashboard `MomentBanner` panels.)

**Type (`tokens/typography.css` must match):** **Poppins** (SemiBold 600) = display/brand - headings H1–H3, the `click` wordmark, primary-button + tab labels, big numbers; Medium 500 for sub-labels. **Body = the system font stack** (`--font-body`) for paragraphs, captions, fields, list rows. **No Inter as the display face; no Fraunces/serif** (retired). Never set paragraphs in Poppins or the wordmark in the body font. **FLUID delivery (locked):** the big tokens are `clamp(MIN, rem + N·cqi, MAX)` - MIN = the recalibrated 375 mobile size, MAX = desktop - so type scales smoothly with the viewport, no device jumps: display 32→64 · h1 28→40 · h2 22→24 · h3 18→20 · `card-title` 17→18 · body-lg 16→17. **Body is a FLAT 16px - never clamped below 16** (iOS zoom + readability); meta 13. (`cqi` not `vw` so the scaled device frames in the mockup demonstrate the fluidity; the app body is a `container-type: inline-size` query container.) **Compact density (locked):** on app surfaces the greeting/page heading = h2, NOT display; reserve display for marketing/landing.

**Imagery:** event/venue = real warm-graded Sydney photography (clay on a wheel, a flat white, runners at dawn). Abstract illustration only for marketing/empty states. NEVER stock photos of faces (members are real; people surfaces stay privacy-careful).

**Cards:** white surface on cream, radius 16–20px, ONE low purple-tinted shadow OR a hairline (not both), no colored left-borders, no cards-inside-cards (group with whitespace, inner ≤ outer).

**Radii:** inputs/buttons ~12px, cards 16–20px, **tags/avatars fully pill/round, badges ~8px rounded RECT** (so a badge never reads as a pill tag).

**Shadows:** soft, low, purple-tinted - never grey, never harsh, **never a glow.** Primary CTAs are flat deep purple.

**Anti-AI-slop (banned):** gradients/glows/mesh, glassmorphism/pervasive blur, cards-inside-cards, everything-centered symmetry, faceless 3D/blobs/stock "diverse people laughing", emoji-as-icons (only ✨/👍 locked), placeholder/lorem data. Light mode only.

**Motion:** calm - short fades, 4–8px entrance rises, ease-out ~120–240ms; hover darken one step; press darken + ~0.98 scale. ✨ only at the three peaks. Honour reduced-motion.

---

## 4. COMPONENTS (`window.ClickDesignSystem_*`)

**Brand marks** (`Logo`, `Cmark`, `AppTile`, `Spark`) - the lowercase `click` wordmark (Poppins SemiBold, the lavender sparkle-pair as the i-dot); `Cmark` the bare `c` cradling the sparkle pair (favicon / avatar basis, holds to 16px); `AppTile` the c-mark on a Deep-Purple squircle (home-screen icon); `Spark` the standalone double-sparkle (a big glint + a small companion) - the one spot of lavender, reserved for the three peaks and the nav "✨ click". Keep ✨ singular, lavender, and never decorative.

**Button** - real CSS states (not JS): default flat Deep Purple / hover `#332873` / pressed `#2A2160` + .985 scale / **focus-visible = 2px Deep-Purple ring + 2px cream offset** (keyboard only) / disabled Mist fill + Slate text (in-DOM) / **loading** = spinner + held width. Sizes 36 (desktop/secondary) · 44 (default, min touch) · 52; **radius 12 - NEVER a full pill**; H-padding 16/20/24. Secondary = white + Mist border → lavender wash on hover. Ghost = text + lavender wash. **No `warm`/apricot variant.** 🔴 **ONE button footprint everywhere** - every CTA on every surface ("See who was there", "Suggest a plan →", "RSVP", "View details", banner actions) uses this same Button (radius 12); never restyle a per-banner pill button.

**Stateful action button (click-with / RSVP)** - ONE footprint across states; only fill + label change, never size/shape. `click with [name]` (filled purple) → **pending "clicked"** (muted Mist/Lavender fill, NO ✨) → **mutual "clicked ✨"** (Sage). Waitlist: "Join waitlist" → "Joined waitlist" (muted). **"Sold out" is a badge/state, never a button.**

**Tag** (`Tag`) - interest TAGS, the chips that live *within* a category (e.g. *Pottery*, *Coffee*, *Wine & bars*, *Run clubs*, *Markets* are TAGS, **never categories** - the 16 categories are the icon-circle system below): ONE neutral look everywhere - white fill, 1px Mist hairline, Ink text, **NO dot**, **height 24 (dense 22), full pill, label 12/500, padding 10 (dense 8), 6px gap**. 🔴 **ONE LINE ONLY** - show as many WHOLE tags as fit the card's content width, then a neutral **"+N"** chip ("Plants · Craft · +2"); never wrap to a second line, never let a tag stretch/break the card width, never shrink the tag font, never a horizontal scroll on a card (narrower card = fewer tags + a bigger N). **Selected (onboarding/filters only) = Deep Purple fill, cream text, NO TICK** (the purple fill is the signal) with a ≥24px tap target - add `aria-pressed`/`aria-selected` so screen readers convey state. Status colour NEVER on a tag. Delete any `soft`/`color` tinting so no surface can re-tint.

**Categories - the canonical 16 (source of truth: `TECH/07_INTEREST_TAGS`)** - the SINGLE category set, used IDENTICALLY on **Discovery, Dashboard, and Onboarding**. A category is NOT a tag (tags live *within* a category); never invent ad-hoc category lists (the old *Pottery / Coffee / Markets / Wine & bars / Run clubs* lists were TAGS misused as categories - removed). This exact set, order, and Lucide icon:
1. Wellness - `Leaf` · 2. Food & Drink - `Wine` · 3. Arts & Crafts - `Palette` · 4. Social - `Users` · 5. Music - `Music` · 6. Fitness & Sport - `Dumbbell` · 7. Outdoors - `Mountain` · 8. Learning - `BookOpen` · 9. Networking - `Briefcase` · 10. Dance - `Music4` · 11. Creative - `Drama` · 12. Lifestyle - `Sparkles` · 13. Community - `Sprout` · 14. Travel - `Map` · 15. Family - `Baby` · 16. **Dating - `Heart` - GATED: render ONLY for users whose intent includes "open to dating"; never shown to a friends-only viewer.**
ONE icon treatment, identical everywhere: **resting** = a Deep Purple `#3B2F81` Lucide LINE glyph (~1.75px stroke, rounded joins) centred on a soft Lavender `#C8B8F8` tint circle (**48px mobile / 56px desktop**), label beneath in the system font 12–13px Ink, centred; **selected/active** = the circle FILLS Deep Purple and the glyph reverses to Cream (selection is ALWAYS Deep Purple, **never** a status colour); **hover (web)** = tint deepens one step; tap target ≥44px. **Monochrome purple-on-lavender ONLY - never rainbow, never a per-category colour.**

**Badge** (`Badge`) - status only, ROUNDED RECT (~8px), height 24, label 12/600, status text on a soft tint per the card-state map. Almost full→Coral · Trending/Waitlist→Amber · Free/You're going→Sage · New→Teal · Sold out→Slate on Mist. **No `warm` tone, no "Founding partner" badge.** The click states ("clicked"/"clicked ✨") are NOT badges.

**Avatar / AvatarStack** - round; initials chip if no photo; non-identifying in social-proof clusters. The "+N" overflow chip is the SAME diameter + vertical centre as the avatars (never floating higher).

**Event Card** (`EventCard`) - 🔴 ONE component, **identical fixed size on every surface** (dashboard, discovery, landing, my-events); every card in a row shares width + height with its internal rows aligned. **Content padding 16px (8pt), grouped rhythm (no open flex-grow void before the footer):** Block 1 `{date eyebrow → title → location}` tight (~4px internal) · **12px gap** · Block 2 `{tags → going row}` (~8px internal) · **16px gap** · footer topped by a **1px Mist hairline + 12px**, price left + CTA right. **Equal-height across a row comes from the 16:9 image + the reserved 2-line title - NOT a stretched gap** (`align-self:start`, never `flex:1`/`margin-top:auto`). Banner 16:9 (`aspect-ratio`, object-fit cover); **DATE shown** in the eyebrow ("Sat 14 Jun · 2:00pm", not just the weekday - ONE line, truncates); title uses the **`--card-title` token (Poppins SemiBold 18/24)**, **2-line MAX clamp via `-webkit-line-clamp:2` (ellipsis, never a hard 1-line cut) and RESERVES 2 lines of height (`min-height:48px`)** so short titles show on one line and rows stay aligned (assume a ~60-char title cap so titles never exceed ~2 lines); **locked location = ONE line: "Suburb · Distance" + a lock glyph only** (the glyph is `flex:none`; explanation is its `aria-label`/tooltip); interest tags **HARD CAP of 3 - then a single "+N" for the rest - AND only as many of those 3 as fit the card width (whichever is fewer); 4+ tags NEVER render 4 chips (it's ≤3 · +N). ONE line, `min-width:0` on the tag row, never wrap, never stretch the card, never shrink the tag font** (via `FitTags max={3}`); price; RSVP. **Going row "+N" = the SAME 26px avatar circle, vertically centred in the flex row (`align-items:center`, `flex:none`) - never a superscript, never raised/smaller/margin-top.** **Every non-title row is exactly one line** (date/location/tags/going each truncate, `min-width:0`). 🔴 **`min-width:0` on the title and tag flex containers** (flex's default `min-width:auto` is what lets long titles/tags force the card wider) so they truncate instead of overflowing. **ALL states (locked / unlocked / waitlist / you're-going / saved) share the SAME spacing, blocks, and footer - they change ONLY the badge, the CTA label, and the venue-reveal**, never the rhythm. Status badge top-left; Save + Share top-right. Venue name/address hidden until booked. Full interest-tag set shows only on the Event Detail page.

**People Card** (`PeopleCard` - `window.CK.PeopleCard`, defined ONCE in `kit.jsx`, used by ALL FOUR surfaces) - the canonical "person you can click with" card. 🔴 **ANATOMY IS INVARIANT on every surface** (no per-surface drift): avatar **LEFT, ONE size ~52** (never shrunk) · **name + intent grouped TIGHT and INLINE** - name card-title ~17 Ink · intent ~13 Slate, sentence case (**never stacked as two lines, never green**) · a **CONDITIONAL commonality hook line that carries a NON-interest axis** (so it NEVER restates the interest tags - that duplication is banned). Pick the FIRST available: (1) shared **EVENT** "You were both at [event]" (pin glyph); (2) shared **MUSIC** "Both into [genres]" (music glyph - the music tags, a different axis from interests); (3) **POST-MUTUAL only**, a light non-sensitive **life tag** "Both new to Sydney / Both pet owners"; (4) cluster **PROXIMITY** "you're both nearby" (computed from a postcode/distance RANGE - never a named suburb/cluster like "inner-west locals"; city-agnostic so it scales); else **OMIT the line** (the interest tags stand alone). 🔴 Life tags are private until mutual, so the pre-mutual discovery / who-was-there card passes `postMutual=false` and shows event/music/proximity only; the post-mutual Your-clicks/reveal card may use the light life tag. 🔴 NEVER the private quiz persona / social-style answers. The line is computed once by `window.CK.commonality(p, postMutual)` / rendered by `CommonalityLine` so all surfaces agree; **≤3 shared neutral tags** (`FitTags`, one line + "+N") · **name only, NO age** (age lives on the profile drawer) · the stateful click button (default → muted "clicked", no ✨ → Sage "clicked ✨") **paired with the quiet "View profile" GHOST** (a ghost button, never a bare text link). The anonymous line shows once at the section top, never per-card. 🔴 **ONLY the action LAYOUT + count vary per surface** (and the attendee list drops the click action): **discovery** = 3 one-per-line full-width rows, actions in a RIGHT column; **dashboard "click with someone"** = 1 rotated card + "See everyone →", same right-column actions (this card previously drifted - stacked name/intent, dropped the commonality line + "View profile", shrank the avatar; it is now the SAME component); **who-was-there** = 2-up grid (1-up mobile), "Show more" past ~12, the action pair in a **bottom row**; **event-unlocked attendee list** = grid, **NO "click with"** (clicking is post-event only) - the WHOLE CARD opens the profile modal, **interests only** (no intent line), mutuals carry the inline "clicked ✨" Sage marker. Only the action layout adapts to width (right-column on wide rows, paired bottom-row on narrow cards); the anatomy and the click-with + View-profile-ghost pair never change.

**IntentLine** (`IntentLine`) - the locked mutual-click intent line: equal intents → "You're both here for [intent]."; mixed → shows both, never collapsed. Sage treatment; sits on the mutual reveal and the Live-mutuals card.

**MutualCard** (`MutualCard`) - the "you clicked with each other" payoff card, **activity-led not couple-y**: a single Deep-Purple ✨ spark glyph in a soft disc + the person's ONE avatar (NO paired/overlapping "you + them" avatars, NO heavy purple gradient - cream card) + the reveal headline ("You clicked with [Name].", Poppins 600) + a Sage intent pill ("You're both here for [intent]", "· both open to dating" ONLY when both have dating on) + up to 2 neutral shared-interest tags + the activity-first line "Find a thing you'd both enjoy, and just show up." + the single "Suggest a plan" action + a silent decline. `preEvent` → "you're both going to [event]"; `postEvent` → "you were both at [event]". Mirrors the in-app reveal modal so the reveal reads identically everywhere. Shown ONCE per user; afterwards tapping the mutual opens the coordination drawer at its current step (never re-fires the reveal).

**Mutual-coordination MODAL set** (5 modals, was 6 - the "Nothing fits just yet" dormant modal is DELETED with dormant). Flow: **reveal (✨ peak) → suggest a plan → you're both going (✨ peak) → [that one just filled up - recovery] → love that (✨ closure peak)**. 🔴 **✨ rationed to the 3 PEAKS only** (reveal / both-going / closure) - suggest + filled-up carry NO ✨ (steps, not peaks; over-use cheapens). 🔴 The ✨ is the single Deep-Purple `#3B2F81` brand glyph everywhere - NEVER orange/multicolour (no literal emoji in headlines). **Reveal (the dopamine peak):** centre of gravity = the ✨ hero glyph + the person's single avatar, NOT couple-y overlapping avatars (never reads as a dating match); soft pop animation, `prefers-reduced-motion` safe; "You clicked with [Name]." (full stop) + a shared-moment line that **surfaces the day** ("You were both at [Event] on [day]") + IntentLine (honest, mixed pairs never collapsed; "both open to dating" folded into the intent line as text, never a coloured tag) + **"Find a thing you'd both enjoy, and just show up."** + primary **"Suggest a plan"** + a quiet **"Maybe later"** dismiss. 🔴 **Shown ONCE** (the instant it goes mutual / next app-open if away; persisted dismissal - never re-opens), then it **becomes the persistent "Suggest a plan" card** on Your clicks + the dashboard your-move banner. Modal = the one-time celebration (snapshot + payoff); card = the durable coordination surface. **Suggest:** neutral proposal tags (white fill, hairline - never lavender-tint); the event card is a **COMPACT Event Card mini** (small ~110px banner thumbnail, not a hero) **tappable → a READ-ONLY preview IN the drawer** (date/suburb/price/tags/what-you-get); the preview's ONLY actions are **"← Back" + a quiet in-place "Save"** (zero-commitment bookmark) - 🔴 NO full-event navigation, NO RSVP/"Continue to payment" sheet (booking happens only after [Name] agrees). NO ✨. **Suggested-to-[Name] WAITING** (proposer, before they reply): "We'll let [Name] know…" + "Back to your clicks"; its event card is **tappable → the FULL Event Detail page** (the suggest decision is already made, so leaving to view the whole event is fine here - unlike the suggest-decision step, which uses the in-drawer read-only preview so you don't lose the thread). 🔴 Still no auto-RSVP - booking happens only after [Name] agrees. **Both-going:** ✨ peak (eyebrow "PLAN CONFIRMED") + **"You're both going."** + "You and [Name] are set for [Event]. See you there." + "Add to calendar"; 🔴 the event card is **TAPPABLE → opens the Event Detail page in its UNLOCKED/booked view** (venue + map + manage/cancel) with a clear "Back to your plan" (matches the suggest card's preview-to-Event-Detail). **Filled-up:** 🔴 NOT an error - neutral lavender-grey recovery icon (NEVER red/coral), **"That one just filled up."** + "No drama - there's always another. Find one you'll both like." + "Find another together" / "Join the waitlist together". **Love that (closure):** ✨ peak (eyebrow "PAST CLICKS") + "Love that." + "That's what Click's for. This one rests in your past clicks - pick it back up anytime.". **Soft-release** (terminal, NOT a peak → neutral glyph, no ✨): "Still out there". All strings locked in `CLICK_LANGUAGE` v22 §5. Premium restraint - dopamine via a real moment, never gamification trinkets (no confetti/badges/streaks) or dark patterns.

**Your-clicks outcome card** - the ONE list-row card on the *click with someone* page, used identically across **Live mutuals · Plans · Past clicks** (avatar 56–64 · the **NAME as the title** · the **intent line grouped TIGHT with the name** (the identity pair - inline beside the name where it fits, else directly beneath; **Slate ~13, sentence case, NEVER green/Sage, NEVER a dotted bullet** - the SAME treatment as the discovery People Card, only the wording differs: "You're both here for [intent]" vs the solo "Here for [intent]". Sage/green is reserved for SUCCESS only - the mutual "clicked ✨" marker, "you're going" - never the intent line) · the action on the **RIGHT, vertically centred & aligned across cards** - the radius-12 Button, never a pill, never a bottom text-link). 🔴 **Content is per-state and framed as what you have IN COMMON, never one-sided pseudo-tags** ("New to Newtown" / "Plays in a band" as standalone chips are banned). 🔴 **No `dormant` state - a live mutual is ALWAYS the actionable "Suggest a plan" card** (the user can always propose); the system's suggestion is **optional content that fills the card when available** ("We think you'd both like [Event] · [day]" + "Suggest it →"), and when there's none yet (the ex-dormant case) the SAME card just shows "Pick something you'd both enjoy" + "Suggest a plan →" - **never empty, never hanging, never a "no match"/"browse together" consolation**. (Backfill-and-notify is preserved as a property of `open` pairs with an empty suggestion list - a TECH concern, not a surfaced state.) A **Live mutual has NO confirmed plan yet → show NO event/calendar line**, only "Both into [shared interests]" + ≤2 mutually-framed shared non-sensitive life tags ("both new to Newtown") + "both open to dating" when both are on. Only the **Plan** card shows a real upcoming event ("Going to [event] · [when]"); **Past** clicks read clearly past ("You clicked at [event]"), never styled like a plan. 🔴 **NO name-adjacent state pill** - never "You two clicked" / "You both clicked" / "You're both going" / "We clicked 🔥" / "Released gently" beside the name; and **NO ✨ on a list row** (✨ is reserved for the three peaks - mutual reveal, both-going, plan-made - never a passive list marker). State is carried by THREE things only: the **section header**, an **earned card accent** (🔴 a soft **`--lavender-wash` (#F0ECF4) full-card FILL on YOUR-MOVE cards only** - `open` "Suggest a plan" + `proposed`-receiver "See their plan"; everything else - `proposed`-proposer "Waiting on …", Plans, Past - stays the clean **neutral white** card; **one emphasis mechanism, NO left-rule, never a full-strength fill** - Slate on the wash = 4.74:1, AA), and the **action** (Live mutual → "Suggest a plan →" into coordination · Plan → opens the plan · Past/connected → **"We clicked 👍"** as a closure *button*, never a status pill · a **soft-released** card carries NO marker - the absence is the signal). Psychology: nudge, never push - never "it's a match"/verdict/hearts; an absence reads as opportunity, not loss. Differentiate by accent, never by a different layout. 🔴 **Whose-turn clarity (same label in all three places - card, drawer, dashboard):** YOUR move = a filled-purple Button with a verb ("Suggest a plan →" · "See their plan →" · "Save your spot →"); THEIR move = a quiet muted "Waiting on [Name]" pill + a calm status line ("Waiting for [Name] to reply" / "to save their spot"). Live-mutual status lines: open → the warmth line · they proposed → "[Name] suggested a plan" · agreed-paid → "[Name]'s in - save your spot" · waiting → muted. **Free events skip the save-spot states; paid events show them.** The **dashboard moment-banner surfaces ONLY your-move states** (reply / save your spot / suggest), most-actionable first - never the waiting states; ✨ only on the fresh-mutual peak. 🔴 **Per-state context line:** Live mutuals (open) → NO event/calendar line (no plan yet - the warmth payoff carries it); Plans → the confirmed event; **Past clicks (connected) → NAME THE PLAN "You went to [Plan] together · [when]"** (fall back to "you clicked at [event]" only if no shared plan). 🔴 **RELEASED ("Still out there") past-clicks COLLAPSE** into a single quiet **"+ N past clicks"** line that expands on tap - never standing faded cards (connected wins stay as visible Past-clicks cards; released ≠ dormant, and dormant is removed). The expanded released entry keeps the neutral "Still out there - if you cross paths again, you can pick it back up", never a loss/"expired" frame.

**AttendeeRow** - NOT a separate component: the event-unlocked attendee list and the who-was-there grid both render the ONE **People Card** above (who-was-there in its click variant, the event attendee list in its no-action / whole-card-opens-profile variant). First name only, never surnames; no life tags, no age.

**Form controls** (`Input`, `Select`, `Toggle`) - `Input` a labelled text field with a lavender focus ring; `Select` a styled native select with a chevron; `Toggle` the on/off switch (Deep Purple when on) used for the visibility / dating-mode settings, with helper text carrying the locked consequence string.

**Merchant components** (`components/merchant/` - the host-business console vocabulary, see the Merchant card):
- **StatCard** - period-scoped KPI tile for the hosting dashboard/finances; max ONE `hero` (Deep Purple) tile per row; money is never "Free" - "$0" + a scope note.
- **CapacityMeter** - the single way capacity renders (confirmed/cap count + slim bar); fill turns Coral above 85% full, otherwise Purple.
- **StatusPill** - the single merchant status vocabulary, wrapping Badge tones: live/paid→Sage · pending/waitlist→Amber · cancelled→Coral · confirmed→Lavender · ended/draft/refunded→neutral.
- **WizardStepper** - numbered progress dots for multi-step merchant flows (become a host, create event); completed steps turn Sage with a check and are clickable, current is Deep Purple.

**Top nav** - responsive sticky header: `click` wordmark (home) · Discover · Dashboard · **✨ click** (people; the ✨ is a standalone leading icon, the only spark in the bar) · "Host an event" · a **notifications BELL** (unread dot that clears on open → a calm, positive-only panel - never a one-way click, see `Click_Design_Prompt_Notifications.md`) · avatar. Mobile: compact header (wordmark + avatar + menu) + a sticky bottom action bar - NOT a wrapping pill row. **Back rule:** top-level pages no back; sub-pages one top-left "← back"; modals/drawers a close (✕) that dismisses the overlay, not the page. 🔴 **App nav = signed-in surfaces only.** Logged-out MARKETING pages (Landing, How-it-works, For-merchants) use a MINIMAL marketing header - no app nav, no repeated big logo - just **Log in** (quiet) + **Sign up** (primary) post-launch; pre-launch needs no header.

**Account menu** (avatar dropdown) - the popover that opens on **tap of the avatar** in the top nav (never hover; `aria-haspopup="menu"` + `aria-expanded`, chevron rotates 180° on open). ONE component, identical on every signed-in surface. White card on cream, radius 16, one soft purple-tinted shadow, ~300px right-aligned under the avatar; on mobile (<768px) the SAME content as a bottom-anchored sheet (grab handle + scrim). Order: a non-interactive **identity header** ("Signed in as [First]" + quiet account line) · hairline · **destinations** (Your profile · People · Your events · Saved) · hairline · **secondary** (How it works · Account settings) · hairline · **Sign out** (quiet Ink/Slate, lavender-tint hover - NEVER error red; sign-out is not destructive). Rows ≥44px (≥48 mobile), lavender-tint hover/focus, radius 12. The **current page's row** shows a lavender-tint + Deep-Purple active state (the only purple moment); no ✨ anywhere. Opens focus to the first item; ↑/↓ move between items; Esc closes AND returns focus to the avatar; tap-outside / scrim / re-tap / selecting an item all close. "Saved" deep-links to My Events → Saved tab; "People" = the click-with-someone page (neutral label, never "Matches").

**Footer** (global, minimal - EVERY page) - same Cream canvas, one Mist hairline, **EXACTLY 2 rows, NO tagline** (remove "Real-life events across Sydney"): row 1 `click` wordmark + essential links (Discover · How it works · Host an event · Help · Privacy · Terms); row 2 "© 2026 Click · Made in Sydney" + social as monochrome line icons (Instagram, Threads - each with `aria-label` + `aria-hidden` glyph) + `hello@click.au` (mailto text). No fat 3-column grid, no Careers, no lavender block, no duplicate host band.

---

## 5. KEY SCREENS

🔴 **ONE signed-in layout container (every page: Dashboard, Discover, click-with-someone, Who-was-there, My Events, Profile, Settings).** A single container max-width ~1060px, **centred** on the cream canvas with consistent ~40px side gutters (~22px mobile). 🔴 The page **TITLE + content are LEFT-ALIGNED at the container's left gutter - the SAME left edge on every page** (verified: identical at every breakpoint). Wide-content pages (Discover grid, My Events) fill the container; **narrow single-column pages (click-with-someone list, Who-was-there header + 2-up grid, the ~660px Profile block) sit LEFT-aligned within the container - capped at their natural width with whitespace falling to the RIGHT, NEVER a narrow column floating centred in the middle.** Mobile (<768) = full-width single column with comfortable gutters.

**Dashboard** - Mode A (first-time, **5 sections**: welcome · **finish-setting-up** · suggested · radar · categories) + Mode B (returning, conditional: post-event prompt · you're going · click-with-someone · click radar · suggested · saved & waitlist · activity · categories). 🔴 **TOP-OF-DASHBOARD MOMENT BANNERS = ONE component, identical layout every state, content only changes** (lavender-tint card · ~44px icon-circle left, Lavender-200 fill + Deep-Purple line glyph · eyebrow → title → one subline · the radius-12 primary action right; mobile stacks the action full-width below). The post-event prompt is one variant; the coordination states are the rest - **post-event** ("last night · [event]" / "Did you click with anyone?" / "No rush - just the people who were there." → See who was there) · **fresh mutual** ("it's mutual" / "You clicked with [Name]. ✨" / "Skip the small talk - pick something, meet there." → Suggest a plan →) · **they proposed** ("from [Name]" / "[Name] suggested a plan" / "[event] · [when]" → See their plan →) · **agreed** ("your plan with [Name]" / "[Name]'s in - RSVP to lock it in" / "[event] · [when]" → RSVP →) · **2+ waiting** → ONE consolidated banner ("your clicks" / "[Name] and [Name] are waiting on you ✨" / "Pick up where you left off." → See your clicks →, routes to the people page). ONE banner at a time (order: post-event → ONE coordination banner); ✨ is the Deep-Purple glyph on the peak titles only (fresh-mutual + consolidated), never on the reply/RSVP variants. Finish-setting-up is the DISTINCT quieter white card, never a third lavender banner. **Finish-setting-up is COMPACT** (pre-filled progress bar + the next incomplete step only, expandable; rows 44–48; a status display, not user-tickable). **Click-with-someone shows EXACTLY ONE rotated person + "See everyone →"** (never 3). **Click Radar = a compact social-proof BAR (NOT event cards)** - 1–3 light rows, each an anonymous aggregate social-proof line tied to an event ("3 people going also like hiking → [event]" · "Mostly people in their 30s going → [event]" · "Trending in Sydney → [event]"), ≥3 floor, never names/photos, taps through to the locked event to RSVP. NOT an event-card strip, NOT a "crossing paths with people" card. Same compact bar on the dashboard and the "click with someone" page.

**Who was there** (post-event click surface, Process 2 - reached from the post-event banner, the ONLY place a post-event click happens) - ONE job: anonymously click with the people who were there. 🔴 **Header on the SAME content grid as the other people/sub pages (one left edge + max-width; never floating in a narrower centred column):** a canonical quiet **"‹ Back" link top-left on its own row** (the same form/position as every sub-page - Settings/Booking), then the page title **"Did you click with anyone?" LEADS** (the Poppins page-title treatment shared with "click with someone"/"Your events" - title-first, consistent with every other page), with the event context as a **quiet line directly UNDER the title** ("✓ Pasta from scratch · you were there · [relative time]" - Slate, NO thumbnail picture; same relative-time logic as the entry banner: earlier today / yesterday / on [weekday] / the date - never a timer), then the subline, the **plain anonymity line** ("Clicking is anonymous - we'll only show you if it's mutual. How clicking works →" - lock glyph + Slate text + link, **NO tinted background**) placed BEFORE the grid, and a quiet **"[N] people were there"** aggregate count. Then the attendee grid: the **canonical People Card, identical to the discovery click page** (avatar **LEFT ~52** · name + intent grouped tight - name ~17 Ink, sentence-case intent ~13 Slate, **not green** · a **CONDITIONAL commonality hook line** where there's overlap - overlap glyph + "Both into [shared]" / pin + "You were both at [event]", omitted cleanly when none · ≤3 shared interest tags at the dense 22/12 size via `FitTags` · a **PAIRED bottom action row**: "click with [Name]" filled Deep-Purple + "View profile" secondary, kept together, never split to opposite corners). **Grid: 1-up mobile / 2-up desktop, 16px gap, equal-height, even ~16-18px padding; "Show more" (load more) past ~12; shared-context people sorted first.** 🔴 **No photo nudge here** (the event has passed) and 🔴 **no "Already swapped a moment / We clicked 👍 / We clicked - suggest something" section** - the "We clicked 👍" closure lives ONLY on an existing mutual's card in Your clicks, never on this page. **Romantic overlay** = an aggregate add-on for dating-mode viewers only ("a few people here are open to dating", ≥3), never on a named card. States: default · empty ("Quiet one") · window-closed · not-eligible. Attendance-gated, anonymous, no timer ever shown.

**Discovery (`/events`)** - the left filter SIDEBAR appears **only ≥1024**; below that (incl. 768) the mobile pattern applies: Filters button → bottom sheet (grab-handle, Reset / ✕ / scrim close, sticky "Show N events" apply). Sortable results grid, same Event Card. The category strip = the **canonical 16** (§4), one icon treatment. Horizontal rails (category row, chip rows, tab rows) hide their scrollbars (`.ckRail`).

**2 Jul audit-fix conventions (site-wide):** modal/drawer resting state is always **visible** - never gate visibility on an entrance animation (re-renders restart CSS animations and freeze content at opacity 0; motion returns later via a re-render-safe mechanism). Product-surface copy is **city-agnostic** - `{area}` / "near you" / "New in town" / "new to the area"; only Landing HERO campaign copy may name the launch city. Mobile event detail shows ONE bottom bar (the booking action bar; the bottom nav hides during that focused task). Onboarding's Continue bar is sticky at the viewport bottom.

**click with someone (Your clicks)** - the people destination: the curated daily pool (the People Card) + Click Radar + the **Your clicks** list grouped *Live mutuals · Plans · Past clicks*, every row the one **Your-clicks outcome card** above (no state pill, no ✨; state via section header + quiet accent + action). The anonymous-clicking reassurance shows once at the top.

**Event Detail** - desktop = TWO columns (content left + sticky booking panel right, NO bottom bar); tablet = single column + in-flow panel; phone = single column + slim price+button sticky bar. 🔴 **The booking panel is the SINGLE home for date/time/location/price/capacity - NO date/time/location strip under the title** (the title goes straight to the category + ALL interest tags at 22/12, then About). **Price = just "$X per person" (or "Free") - never "RSVP, not a ticket".** **Grouped 8pt rhythm** (title → tags → about → who's going), title is the focal point, even padding, no airy voids. **locked** = suburb + lock + capacity bar + status tag; **Who's going (locked)** is a COMPACT card - avatar cluster + count on one line, then AT MOST TWO aggregate social-proof lines **leading with "A few people you might click with are going"** (≥3, never names/photos), then the one-line anonymity note. **unlocked (booked)** = venue name + address + a real static map (NO "venue unlocked" eyebrow - lead with "You're going ✓" Sage check), **"Add to calendar" = ONE button + calendar icon → menu** (Google/Apple/Outlook/.ics, never 3 inline pills), Manage +1s, Share, and a **quiet "Cancel RSVP" text link** whose confirm shows the real refund and uses the destructive red **`#B5362F`** (never Coral). **Attendees (unlocked only)** = a calm grid where the **WHOLE card is clickable → profile drawer (NO "view profile" button, a quiet chevron signals it)**; pic · first name · ≤3 shared interest tags at 22/12; **NO "click with"**, NO life tags; if the viewer has an active MUTUAL with an attendee, a small Sage **"clicked ✨"** marker (one Spark glyph); "open to dating" only under mutual opt-in; **no "How clicking works →" here**. **Photo nudge = UNLOCKED/booked ONLY, never on the locked page.** waitlist = position + ~30-min claim offer; venue stays locked. RSVP → +1-up-to-4 stepper → Stripe (free = one tap) as progressive disclosure in the panel. Full interest-tag set shown under the title. NO "click with" button anywhere on the page. 🔴 **Reached from a click PROPOSAL** (the coordination drawer's "Save my spot · RSVP" deep-links here): a top **"RSVP to lock in your plan with [Name]"** banner shows; on a confirmed RSVP the user **STAYS on this page in its unlocked/booked state** (the banner flips to **"You're going with [Name] - we'll let them know"**) - it does NOT bounce back to the coordination drawer.

**RSVP / booking modal** (opened from the Event Detail panel + mobile sticky bar) - 🔴 the panel's **RSVP button OPENS A MODAL** (one booking surface); the panel never expands/checks-out in place. The modal: event recap header + ✕ · a **"When you're in, you'll get"** value block (exact spot + map · who's going · add-to-calendar; outcome-led, never "unlock to see") · **"Bringing anyone?"** (0-4 seats, each optionally **named** → first name + email + DOB, required *once named*, 18+ inline gate, no last name/postcode; an unnamed seat is a frictionless **+1**) · a **consent checkbox + one-line disclosure** shown only when ≥1 seat is named (both block confirm). **Paid** shows the cancellation line **"Full refund up to 48h before - 50% within 48h - none within 24h"** + "Secure checkout · powered by Stripe", and **"Continue to payment · $X"** → Stripe **hosted-redirect** (default, canon 05) → returns to the **unlocked** Event Detail. **Free** = in-modal "RSVP" → success ("You're going" ✓, Sage not ✨) → "View event" → closes → unlocked page. Esc / ✕ / scrim dismiss; mobile = bottom sheet. Naming OPTIONAL (OAIC APP 3); DOB is the 18+ check only. 🔴 **Contrast + elevation (not washed-out):** a **definite Ink scrim `rgba(28,24,48,0.5)`** behind (the card must clearly separate from the cream page), a **PURE WHITE `#FFFFFF` card** (not cream), and a **real soft shadow `0 12px 32px rgba(28,24,48,.14) + 0 2px 6px rgba(28,24,48,.08)`** (lift, never a glow). 🔴 **NO tinted inner panels** - the "Bringing anyone?" stepper and the value block sit on white, separated by whitespace + at most a 1px Mist hairline (the only tinted surface allowed is the coordination "RSVP to lock in your plan with [Name]" banner). 🔴 **Four-level tonal hierarchy:** white card → **Ink anchors** (price `$X` Poppins SemiBold ~24-28 with small Slate "per person" beside it; event title; each value-row TITLE) → **Slate support** (date/time/suburb meta, value-row sub-lines, the quiet micro-caps eyebrow) → one **Deep-Purple CTA**. Value-row icons = full-strength Deep-Purple `#3B2F81` glyph on a light Lavender circle. Never set a value row's title AND sub both in Slate (that flatness is the wash).

**Landing** (logged-out marketing) - the signature is a REAL dictionary entry: headword `click` ✨ · "/klɪk/ · verb" · a numbered sense ("1. to connect effortlessly with someone…") · an **italic example sentence** ("we met at pickleball and just clicked!") · a Mist rule · the positioning line. Minimal marketing header (no app nav): pre-launch = no header + one **"Request an invite"** CTA (email + suburb/postcode; out-of-area is never blocked - "Somewhere else in Sydney? Join anyway…"); post-launch = **Log in + Sign up** top-right + the hero "Get in" CTA + a 3-event strip *below* the hero (warm activity photos). The minimal 2-row footer (no stacked "Host on Click" band - the footer's "Host an event" carries supply).

**How it works / Onboarding** - **How It Works** is a calm, warm, editorial SCROLL that *sells the positive* (desire + curiosity + the proximity effect), not a feature grid: hero desire-headline ("The best people you'll meet this year aren't on an app. They're across the room.") + "Request an invite" · why-it-works (proximity) · the 3-step model · **the click** (teased, never explained; no rejection in either direction; one ✨ on the page) · "on purpose" (no swiping/chat) · why-it-stays-calm trust trio · who-it's-for (desire-led, never labelling the reader) · close. Onboarding = 4-step + done progressive profiling (postcode, multi-select intents with inline dating sub-questions + the visibility toggle only when "Open to dating", category-icon interests grid, optional photo) with endowed progress + a spot illustration per step. **Dating mode** is first set here (when "Open to dating" is chosen); its on/paused toggle otherwise lives in **Settings / Edit Profile** - NOT in the dashboard header.

**Settings / Edit Profile** - **Settings** is ONE page with four sections (Edit profile · Privacy & visibility · Account · Notifications); the profile's "Edit profile" button deep-links here. **Edit profile** holds profile CONTENT: photos, about you, bio, intents, and 🔴 **interest TAGS + music TAGS - never the 16 category circles** (a profile selects the specific things inside a category - Pottery, Run clubs, Wine tasting, Plants - using the canonical neutral→purple-fill `Tag` pill, grouped under quiet category headings, 8 categories by default + "Show more interests", Dating-category tags gated to dating-intent users; music is a separate, lighter, optional 25-genre block below interests). The **Open-to-dating** intent reveals an inline **Dating mode: On/Paused** toggle (`romantic_visible`) right there with one warm line ("Only people also open to dating ever see this") - the *detailed* dating preferences (interested-in / age range, private) live in **Privacy & visibility**, alongside the "Show me in event attendee lists" opt-out. Edit profile also carries a quiet **Click quiz** row (Take / Update → opens the quiz modal; no ✨ on the row).

**The Click quiz** - a **full-screen MODAL takeover** (dimmed page behind, NOT a route; X / Esc close, returns you exactly where you were; auto-saves per step, resumable). Top bar = `click` wordmark · "Step X of 5" + a slim bar · close. **Endowed progress** (pre-filled ~22% on step 1, fast early). Reordered **fun-first / sensitive-last, 5 steps**: 1 Your kind of room · 2 How you connect · 3 What you're after lately (intent-neutral "what brings you to Click?" multi - "Open to dating" is 1 of 6 and **last**; selecting it slides in a gated dating sub-block: who you'd like to meet + an age-range two-thumb slider + "only shapes who we suggest, never shown on your profile") · 4 Your week & range (availability + distance) · 5 A little about you (life stage · pet · LGBTQ+ - optional, "Prefer not to say", soft line "Optional and private to you - it helps us keep events welcoming", **no community-signal/"we tell others" copy**). **Every question optional/skippable.** Option pills = one neutral style (white / Mist hairline / Ink), **12px radius, ≥44px tap, selected = Deep-Purple fill + cream label, NO tick** (`aria-pressed`); set the `background-color` longhand, never a transitioned `background` shorthand (which leaves selected pills unpainted). 🔴 **NO spark glyph anywhere on quiz surfaces** (spark is reserved for the three mechanic peaks) - section/intro/finish use plain Lucide glyphs on a Lavender circle. Hyphens, never em-dashes. **Results are silent** - the finish screen ("You're all set") never lists the user's life tags; it shows a tuned **"What's on near you this week"** strip using the canonical Event Card + "See what's on". Reachable from the dashboard "Finish setting up" → "Take the Click quiz" task. **Deliberately removed:** the disguised age question, fragile-state tags (recently single / navigating a big change), the living-situation question, and the old LGBTQ+ community-signal explainer.

**My Events** - Upcoming · Waitlist · Saved · Past + List/Calendar (calendar only here).

---

## 6. PRIVACY & MECHANIC RAILS

No chat, ever - a mutual → structured "suggest an event" only. **The coordination drawer is ONE progressing flow: suggest a plan → wait for their yes → both RSVP → both going.** 🔴 **Proposing is NOT booking** - suggesting a plan never RSVPs anyone; each person saves their own spot (free = both confirm instantly; paid = each pays), and "you're both going" only after both have. ✨ peaks only (both-going + the reveal), the Deep-Purple `Spark` glyph never the orange emoji; never framed as a date/"match". Anonymous until mutual; never reveal a one-way click; no "likes you" queue. **Life tags are silent** - never shown on a profile or attributed to a named person; they power AGGREGATE FOMO/radar copy ("3 people over 50 going", ≥3, non-sensitive only) and the post-mutual "we're the same" shared-non-sensitive-tag snapshot (gated on an active mutual). 🔴 **"Open to dating" (any label/line, ANYWHERE - people cards, mutual cards, attendee lists, profiles) displays ONLY when BOTH the viewer and the other person have dating mode on** - a friends-only viewer NEVER sees a dating label anywhere (intent-neutral + mutual opt-in). On event pages the aggregate romantic FOMO is shown only to dating-viewers (≥3). Sensitive life tags never shown, even in aggregate. **Intent-neutral - stated positively, never by negation:** name what each person *wants* - friends, something more, or just a fuller social life - all equal and all working the same way; foreground friends/activities/networking (≤1 dating per set of 3); meeting is activity-first and the mutual reveal leads with the connection + the shared activity. Say the positive ("come for the thing you love, see who you meet"), not the negation that plants the dating frame ("never a match", "no swiping/singles/hearts"). **Microcopy:** clear, concise, never over-explain or reveal the internals (no refresh timers, no "a one-way click is never shown", no "re-click anytime") - keep the magic + curiosity.

---

## 7. REWARD PSYCHOLOGY (tasteful - never a dark pattern)

Endowed progress (bars start pre-filled, never 0). Variable-but-finite daily set (3 people/day, the rotated person, radar) = a curiosity gap worth returning for. Intrinsic over extrinsic. A natural "you're done for today" stopping point. ✨ only at the three peaks. **Banned:** depleting counters ("2 clicks left"), streaks, points, leaderboards, loss/urgency, guilt nudges, fake scarcity.

---

*Source of truth: `cndykm/click-tech` → `UIUX/` (canonical specs + `Cowork/` per-screen prompts). On conflict, canon wins and this README is corrected to match. Owner: Cindy · Implementation: Doan.*
